workflow:
  name: output masking credential echo
  vars:
    credential_x: INTEGRATION_CREDENTIAL_X_SECRET
    lab_label: integration-visible
  tasks:
    - name: nornflow.echo
      args:
        msg: "credential_x={{ credential_x }} lab_label={{ lab_label }}"
